apiVersion: v1
kind: Service
metadata:
  name: nebula-cart
  namespace: nebula-cart
spec:
  selector:
    app: nebula-cart
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30080
  type: NodePort
